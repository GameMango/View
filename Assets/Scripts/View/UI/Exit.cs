using UnityEngine;

namespace View.UI
{
    public class Exit : MonoBehaviour
    {
        // Start is called before the first frame update
        public void quit()
        {
            Application.Quit();
        }
    }
}
